@model BlOO.ViewModels.PostViewModel

@{
    ViewData["Title"] = "Edit Post";
}

<h2>Edit Post</h2>

<form asp-action="SaveEditedPost" method="post">
    <input type="hidden" asp-for="Id" />

    <div class="form-group">
        <label asp-for="Content">Post Content</label>
        <textarea asp-for="Content" class="form-control"></textarea>
    </div>

    <div class="form-group">
        <label asp-for="ImgUrl">Image URL</label>
        <input asp-for="ImgUrl" class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary">Save Changes</button>
    <a href="@Url.Action("Post", "Post", new { id = Model.Id })" class="btn btn-secondary">Cancel</a>
</form>
